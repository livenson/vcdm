VCDM

Includes drafts of:
- CDMI server 
- CMDI clients (.Net, Java, curl, python)
- buildbot conf
- docs
- sample server credentials (!!! never use them in production !!!)

Each should get its own git repo.
